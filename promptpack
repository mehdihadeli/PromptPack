#!/usr/bin/env bash
# Repo-local development launcher for the PromptPack CLI.
set -euo pipefail

script_dir="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
project="$script_dir/src/PromptPack.Cli/PromptPack.Cli.csproj"

dotnet run --project "$project" --no-restore -v:q -- "$@"
